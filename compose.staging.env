# Staging env。容器内部端口同 prod（8000/8080），docker-compose.staging.yml 改外层映射。
BACKEND_PORT=8000
SEARXNG_PORT=8080
HTTP_PORT=80
HTTPS_PORT=443

BACKEND_INTERNAL=http://backend:8000
SEARXNG_INTERNAL=http://searxng:8080

DOMAIN=staging-deeppin.duckdns.org
